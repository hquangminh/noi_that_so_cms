import SectionContent from 'components/Fragments/SectionContent';

const MediaSearch = () => {
  return <SectionContent title='Công cụ tìm kiếm'>{null}</SectionContent>;
};

export default MediaSearch;
